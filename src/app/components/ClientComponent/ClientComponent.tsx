'use client'
import React from "react";
const ClientComponent = ({children}: {
    children: any
}) => {
    return (
        <>
            {children}
        </>
    );
};

export default ClientComponent;